# instruments-service Development Rules

**Repository Context**: This is one of 30+ independent repositories in a multi-repo workspace (not a monorepo).

This is instruments-service: canonical instrument definitions for CeFi, TradFi, and DeFi across all venues.

## Codex References

This service follows unified-trading-codex principles:
- 01-domain/batch/per-service/instruments-service.md
- 01-domain/live/per-service/instruments-service.md
- 04-architecture/batch-live-symmetry.md, concurrency.md
- Plus all universal principles in codex top-level READMEs

## Workspace Rules

Inherits all rules from workspace `.cursorrules`: quality gates, quickmerge (with Cursor Team Kit for CI/reviews), config standards, cloud-agnostic design, event logging, parallel agent execution, rule amnesia detection.

**⚠️ RUNTIME VERIFICATION (P0 BLOCKING GATE):** NEVER claim "done" without actual runtime verification. For backend: must start service, wait 8-10s, check for import/startup errors, verify service starts cleanly. See `.cursor/rules/runtime-verification-required.mdc`.

**Parallel Agents**: For independent tasks (different services, non-overlapping files), automatically launch 2-4 parallel agents. See `.cursor/rules/parallel-agent-execution.mdc`.

**Rule Amnesia Detection**: If agent uses `os.getenv()`, `pip install`, `git push main`, or suggests skipping tests → start fresh session. See `.cursor/rules/rule-amnesia-detection.mdc`.

**Cursor Team Kit**: Use `/deslop` before committing. **Agent automatically starts ci-watcher after quickmerge** (no user prompt needed).

## Python and Package Manager

- **Python**: 3.13 (`.python-version`, `pyproject.toml`, CI, Cursor `python.defaultInterpreterPath`)
- **Package Manager**: UV only. NEVER use `pip install` (except `pip install uv` bootstrap).
- Use `uv pip install -e ".[dev]"` locally; `uv pip install --system -e ".[dev]"` in Docker.

## Quality Gates (Three-Stage Consistency)

**CRITICAL**: Same `ruff==0.15.0` across all three stages:
1. **Local**: `pyproject.toml` dev deps + pre-commit hooks (`v0.15.0`)
2. **GitHub Actions**: `.github/workflows/quality-gates.yml` (`ruff==0.15.0`)
3. **Cloud Build**: Tests run INSIDE Docker image with `.[dev]` deps

**Package Manager**: Use `uv` for all installs:
```dockerfile
RUN uv pip install --system -e ".[dev]"
```

**Cloud Build Architecture** (test-in-image):
- Pull `unified-cloud-services:latest` base (requires auth step)
- Build image (includes source + tests + dev deps)
- Run `scripts/quality-gates.sh --no-fix --quick` inside image
- Push ONLY if tests pass

Verify consistency: `cd unified-trading-deployment-v2 && ./scripts/check-ruff-versions.sh`

## Service-Specific Patterns

- **Venue adapter plugin pattern**: Lazy loading per venue (Tardis, Databento, The Graph, protocol SDKs).
- **corporate-actions as embedded sub-mode**: Runs as `--mode corporate_actions` (same Docker image, TradFi equities only: dividends, splits, earnings).
- **Selective venue initialization**: Only connect to venues specified in CLI args; never fetch all venues at once.
- **MAX_WORKERS: 16** (I/O-bound venue API calls; can over-subscribe CPU).
- **Category-specific buckets**: CEFI, TRADFI, DEFI each have dedicated GCS buckets.

## Service-Specific Anti-Patterns

- NEVER fetch all venues at once; use selective init via `--venues`.
- NEVER use `os.getenv()` for config; extend InstrumentsServiceConfig.
- ALWAYS validate `instrument_id` format before writing to GCS.
- corporate-actions is TradFi equities only (not CeFi/DeFi).

## Dependencies

- **Upstream**: None (root service).
- **Downstream**: All services (market-tick-data-handler, market-data-processing, features, strategy, execution).
- See: unified-trading-deployment-v2/configs/dependencies.yaml


Append "use context7" when working with:
- Tardis API (instrument metadata, exchange mappings)
- Databento API (TradFi symbology, datasets, parent symbols)
- The Graph (DeFi subgraphs, Uniswap, Aave)

## Testing

- tests/unit/test_event_logging.py MUST exist.
- Use synthetic fixtures for venue adapters; mock external APIs.
- validate_timestamp_date_alignment() before every GCS write.

---

## SSOT Alignment Validation

**CRITICAL**: This service's patterns must stay aligned with:
- UTD v2 configs (`unified-trading-deployment-v2/configs/`)
- Codex standards (`unified-trading-codex/`)
- Epics (`unified-trading-codex/11-project-management/epics/`)

### Before Committing Changes That Affect:

- Sharding dimensions or compute resources
- Data dependencies (upstream/downstream services)
- MVP scope or data coverage
- Service metadata or deployment topology
- Configuration patterns or event logging

**Run alignment validation**:

```bash
cd unified-trading-codex
python scripts/validate-alignment.py --check-drift
# Exit code 0 = aligned, 1 = drift detected
```

If drift detected, update related sources:

1. **Sharding changes** → Update:
   - `unified-trading-deployment-v2/configs/sharding.{service}.yaml`
   - `unified-trading-codex/04-architecture/batch/per-service/{service}.md`
   - This `.cursorrules` file

2. **Data dependency changes** → Update:
   - `unified-trading-deployment-v2/configs/dependencies.yaml`
   - `unified-trading-codex/02-data/batch/per-service/{service}.md`
   - Service-registry.yaml upstream_dependencies

3. **MVP scope changes** → Update:
   - `unified-trading-codex/11-project-management/mvp-universe.yaml`
   - Epic specifications if applicable

### SSOT Reference

See: `.cursor/rules/ssot-alignment-enforcement.mdc` for complete alignment rules.

**Last alignment validation**: Run `validate-alignment.py --check-drift` before every commit.


## External Import Standards

**CRITICAL: Always use top-level imports for external unified libraries**

### Required Import Pattern

```python
# ✅ CORRECT - Top-level imports only
from unified_config_interface import UnifiedCloudConfig, ConfigValidator
from unified_events_interface import setup_events, log_event
from unified_domain_services import InstrumentsDomainClient
from unified_cloud_services import get_storage_client
from unified_market_interface import get_market_client
from execution_algo_library import TWAP, VWAP

# ❌ WRONG - Never use deep/nested imports for external libraries
from unified_config_interface.core.config import UnifiedCloudConfig
from unified_events_interface.logging.events import log_event
from unified_domain_services.clients.instruments import InstrumentsDomainClient
```

### Auto-Fix Violations

```bash
# Check for violations
python3 .cursor/scripts/check-import-patterns.py

# Auto-fix all violations
python3 .cursor/scripts/check-import-patterns.py --fix

# Fix through quality gates
bash scripts/quality-gates.sh --fix

# Fix specific directory
python3 .cursor/scripts/check-import-patterns.py --fix src/
```

### Why This Matters

1. **Refactoring Flexibility**: Allows upstream packages to reorganize without breaking consumers
2. **Cleaner APIs**: Makes it clear what's publicly available
3. **Easier Discovery**: Developers can easily find available classes/functions
4. **Reduced Coupling**: Avoids coupling to internal implementation details

### Enforcement

- Quality gates will fail if deep imports are detected
- Pre-commit hooks prevent committing violations
- Use `--fix` flag with quality gates or import checker to auto-correct

### Reference

See: [External Import Standards](../unified-trading-codex/06-coding-standards/external-import-standards.md)
