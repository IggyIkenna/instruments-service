name: Quality Gates

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  quality-gates:
    uses: IggyIkenna/unified-trading-pm/.github/workflows/python-quality-gates.yml@live-defi-rollout
    with:
      dep_repos: "unified-api-contracts unified-trading-library unified-config-interface unified-events-interface unified-domain-client unified-market-interface unified-cloud-interface unified-reference-data-interface unified-internal-contracts"
    secrets:
      GH_PAT: ${{ secrets.GH_PAT }}
