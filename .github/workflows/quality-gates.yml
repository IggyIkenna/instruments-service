name: Quality Gates

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch: {}

jobs:
  quality-gates:
    uses: IggyIkenna/unified-trading-pm/.github/workflows/python-quality-gates.yml@live-defi-rollout
    with:
      dep_repos: "unified-api-contracts unified-cloud-interface unified-config-interface unified-domain-client unified-events-interface unified-internal-contracts unified-market-interface unified-ml-interface unified-reference-data-interface unified-trading-library"
    secrets:
      GH_PAT: ${{ secrets.GH_PAT }}
      GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
