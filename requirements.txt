# Instruments Service Requirements

# Core dependencies
pydantic>=2.0.0
pandas>=2.0.0
python-dateutil>=2.8.0
numpy>=1.24.0
requests>=2.28.0
ccxt>=4.1.77

# Data provider integrations
databento>=0.20.0  # TradFi instruments (CME, NASDAQ, NYSE)

# Unified Cloud Services (direct dependency - per unified architecture plan)
# Install from local repo (editable install when repo is cloned):
#   pip install -e ../unified-cloud-services
# Or from GitHub (when package is published):
#   pip install git+https://github.com/IggyIkenna/unified-cloud-services.git
#
# Package details:
# - PyPI name: unified-cloud-services (hyphens, from pyproject.toml)
# - Import name: unified_cloud_services (underscores, Python package directory)
# - Provides: create_market_data_service, CloudTarget, SecretManagerClient, etc.
# - Per unified architecture: all services use unified-cloud-services directly

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0







