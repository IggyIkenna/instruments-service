name: Quality Gates

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality-gates:
    uses: IggyIkenna/unified-trading-pm/.github/workflows/python-quality-gates.yml@live-defi-rollout
    with:
      dep_repos: "unified-api-contracts unified-cloud-interface unified-config-interface unified-domain-client unified-trading-library unified-internal-contracts unified-market-interface unified-ml-interface unified-reference-data-interface unified-trading-library"
    secrets: inherit
